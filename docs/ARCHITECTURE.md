# 🏗️ COPCCA CRM - System Architecture

## 📊 High-Level Overview

```
┌────────────────────────────────────────────────────────────────┐
│                         USER'S DEVICE                          │
│                    (Desktop or Mobile)                         │
│                                                                │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │           Web Browser (Chrome, Firefox, etc.)            │ │
│  │                                                          │ │
│  │  ┌───────────────────────────────────────────────────┐  │ │
│  │  │         COPCCA CRM React Application              │  │ │
│  │  │                                                   │  │ │
│  │  │  - Login/Signup                                   │  │ │
│  │  │  - Dashboard                                      │  │ │
│  │  │  - After-Sales Follow-up                          │  │ │
│  │  │  - KPI Tracking                                   │  │ │
│  │  │  - Competitor Info                                │  │ │
│  │  │  - Sales & Marketing                              │  │ │
│  │  │  - Debt Collection                                │  │ │
│  │  │  - Task Management                                │  │ │
│  │  │  - AI Assistant                                   │  │ │
│  │  └───────────────────────────────────────────────────┘  │ │
│  └──────────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────────┘
                              │
                              │ HTTPS
                              ▼
┌────────────────────────────────────────────────────────────────┐
│                      DIGITALOCEAN                              │
│                     App Platform                               │
│                                                                │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │                  Static File Hosting                     │ │
│  │                                                          │ │
│  │  - Serves built React app (HTML, CSS, JS)              │ │
│  │  - Auto-deploys from GitHub                            │ │
│  │  - Environment variables injected at build time        │ │
│  │  - SSL/HTTPS enabled                                   │ │
│  │  - CDN for fast delivery                               │ │
│  └──────────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────────┘
                              │
                              │ API Calls (HTTPS)
                              ▼
┌────────────────────────────────────────────────────────────────┐
│                        SUPABASE                                │
│              Backend-as-a-Service Platform                     │
│                                                                │
│  ┌──────────────────┐  ┌──────────────────┐  ┌─────────────┐ │
│  │  Edge Function   │  │   Auth Service   │  │  Database   │ │
│  │  make-server-    │  │                  │  │  Postgres   │ │
│  │  a2294ced        │  │  - Login/Signup  │  │             │ │
│  │                  │  │  - JWT tokens    │  │  Tables:    │ │
│  │  Routes:         │  │  - Sessions      │  │  - kv_store │ │
│  │  - /health       │  │  - Password      │  │  - auth.*   │ │
│  │  - /api/*        │  │    reset         │  │  - storage.*│ │
│  │  - /whatsapp/*   │  │  - Social login  │  │             │ │
│  │  - /tasks/*      │  │                  │  │             │ │
│  │  - /reports/*    │  │                  │  │             │ │
│  │  - /subscription │  │                  │  │             │ │
│  └──────────────────┘  └──────────────────┘  └─────────────┘ │
│                                                                │
│  ┌──────────────────┐  ┌──────────────────┐                  │
│  │     Storage      │  │   Realtime       │                  │
│  │                  │  │                  │                  │
│  │  - File uploads  │  │  - Subscriptions │                  │
│  │  - Blob storage  │  │  - Live updates  │                  │
│  └──────────────────┘  └──────────────────┘                  │
└────────────────────────────────────────────────────────────────┘
```

---

## 🔐 Authentication Flow

```
User Opens App
     │
     ▼
┌──────────────────┐
│  Login Screen    │
└──────────────────┘
     │
     ▼ (enters email/password)
┌──────────────────────────────────────┐
│  Supabase Auth Service               │
│  - Validates credentials             │
│  - Creates session                   │
│  - Returns JWT access token          │
└──────────────────────────────────────┘
     │
     ▼ (access token)
┌──────────────────────────────────────┐
│  Frontend stores token               │
│  - localStorage                      │
│  - Used for all API calls            │
└──────────────────────────────────────┘
     │
     ▼ (subsequent requests)
┌──────────────────────────────────────┐
│  All API calls include:              │
│  Authorization: Bearer <token>       │
└──────────────────────────────────────┘
```

---

## 📡 Data Flow Example: Adding a Follow-Up Record

```
┌──────────────────────────────────────────────────��──────────────┐
│ STEP 1: User fills out form in After-Sales module               │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│ STEP 2: React component calls API                               │
│   fetch('/api/followup', {                                      │
│     method: 'POST',                                             │
│     headers: { Authorization: 'Bearer <token>' },               │
│     body: JSON.stringify({ customer, amount, ... })             │
│   })                                                            │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│ STEP 3: Request goes to Supabase Edge Function                  │
│   https://bpydcrdvytnnjzytkptd.supabase.co/functions/v1/        │
│   make-server-a2294ced/api/followup                             │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│ STEP 4: Edge Function validates auth                            │
│   - Extracts JWT token                                          │
│   - Calls Supabase Auth to verify                               │
│   - Gets user ID                                                │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│ STEP 5: Edge Function saves to database                         │
│   await kv.set('followup_123', {                                │
│     userId,                                                     │
│     customer,                                                   │
│     amount,                                                     │
│     createdAt: new Date()                                       │
│   })                                                            │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│ STEP 6: Database stores data                                    │
│   kv_store_a2294ced table:                                      │
│   ┌──────────────┬────────────────────────────┐                │
│   │ key          │ value                      │                │
│   ├──────────────┼────────────────────────────┤                │
│   │ followup_123 │ { userId, customer, ... }  │                │
│   └──────────────┴────────────────────────────┘                │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│ STEP 7: Response sent back to frontend                          │
│   { success: true, id: 'followup_123' }                         │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│ STEP 8: UI updates with new record                              │
│   - Record appears in list                                      │
│   - Success message displayed                                   │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🗂️ Database Structure

### Key-Value Store (kv_store_a2294ced)

```
┌─────────────────────────────────────────────────────────────┐
│                    kv_store_a2294ced                        │
├─────────────────┬───────────────────────────────────────────┤
│ Column          │ Type                                      │
├─────────────────┼───────────────────────────────────────────┤
│ key             │ TEXT (PRIMARY KEY)                        │
│ value           │ JSONB                                     │
│ created_at      │ TIMESTAMP                                 │
│ updated_at      │ TIMESTAMP                                 │
└─────────────────┴───────────────────────────────────────────┘
```

### Data Organization by Prefix

```
users:*
  └── users:alice@example.com
      { name, email, role, company, subscription, ... }

followups:*
  └── followups:user123:record1
      { customer, amount, date, status, ... }

kpis:*
  └── kpis:user123:2024-12
      { revenue, customers, growth, ... }

competitors:*
  └── competitors:user123:competitor1
      { name, products, pricing, notes, ... }

strategies:*
  └── strategies:user123:strategy1
      { title, category, content, ... }

debts:*
  └── debts:user123:debt1
      { customer, amount, dueDate, followUps, ... }

tasks:*
  └── tasks:user123:task1
      { title, description, dueDate, completed, ... }

subscriptions:*
  └── subscriptions:user123
      { plan, status, amount, paidUntil, ... }
```

---

## 🔄 Environment Variables Flow

### Build Time (DigitalOcean)

```
┌──────────────────────────────────────┐
│  DigitalOcean Environment Variables  │
│                                      │
│  VITE_SUPABASE_URL                   │
│  VITE_SUPABASE_ANON_KEY              │
│  VITE_SUPABASE_PROJECT_ID            │
│  VITE_SUPABASE_SERVICE_ROLE_KEY      │
└──────────────────────────────────────┘
              │
              ▼ (during build)
┌──────────────────────────────────────┐
│  npm run build                       │
│  - Vite replaces import.meta.env.*   │
│  - Values hardcoded in bundle        │
└──────────────────────────────────────┘
              │
              ▼
┌──────────────────────────────────────┐
│  Built JavaScript files              │
│  - Contains Supabase URL             │
│  - Contains anon key                 │
│  - Contains project ID               │
└──────────────────────────────────────┘
```

### Runtime (Supabase Edge Function)

```
┌──────────────────────────────────────┐
│  Supabase Secrets                    │
│                                      │
│  SUPABASE_URL                        │
│  SUPABASE_ANON_KEY                   │
│  SUPABASE_SERVICE_ROLE_KEY           │
│  SUPABASE_DB_URL                     │
└──────────────────────────────────────┘
              │
              ▼ (at runtime)
┌──────────────────────────────────────┐
│  Edge Function code                  │
│  Deno.env.get('SUPABASE_URL')        │
└──────────────────────────────────────┘
```

---

## 🚀 Deployment Pipeline

```
┌──────────────────────────────────────────────────────────────┐
│ STEP 1: Developer makes changes locally                      │
│   - Edit code in VS Code                                     │
│   - Test with `npm run dev`                                  │
│   - Build with `npm run build`                               │
└──────────────────────────────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────────┐
│ STEP 2: Commit to Git                                        │
│   git add .                                                  │
│   git commit -m "Add feature"                                │
└──────────────────────────────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────────┐
│ STEP 3: Push to GitHub                                       │
│   git push origin main                                       │
└──────────────────────────────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────────┐
│ STEP 4: DigitalOcean detects push                            │
│   - Webhook from GitHub                                      │
│   - Starts new deployment                                    │
└──────────────────────────────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────────┐
│ STEP 5: DigitalOcean builds                                  │
│   - Clone repo                                               │
│   - npm install                                              │
│   - npm run build                                            │
│   - Inject environment variables                             │
└──────────────────────────────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────────┐
│ STEP 6: DigitalOcean deploys                                 │
│   - Upload dist/ folder to CDN                               │
│   - Configure SSL                                            │
│   - Update DNS                                               │
└──────────────────────────────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────────┐
│ STEP 7: App is live!                                         │
│   https://your-app.ondigitalocean.app                        │
└──────────────────────────────────────────────────────────────┘
```

### Separate: Edge Function Deployment

```
┌──────────────────────────────────────────────────────────────┐
│ Deploy Edge Function (manual)                                │
│   supabase functions deploy make-server-a2294ced             │
└──────────────────────────────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────────┐
│ Supabase uploads function                                    │
│   - Packages Deno code                                       │
│   - Deploys to edge network                                  │
│   - Makes available at URL                                   │
└──────────────────────────────────────────────────────────────┘
```

---

## 🌐 Request Flow Diagram

### User Login Request

```
User Browser                 DigitalOcean              Supabase
    │                             │                       │
    │ 1. Load app                 │                       │
    ├────────────────────────────>│                       │
    │                             │                       │
    │ 2. Return HTML/CSS/JS       │                       │
    │<────────────────────────────┤                       │
    │                             │                       │
    │ 3. POST /auth/sign-in       │                       │
    │    (email, password)        │                       │
    ├──────────────────────────────────────────────────────>│
    │                             │                       │
    │                             │  4. Validate          │
    │                             │     credentials       │
    │                             │                       │
    │ 5. Return JWT token         │                       │
    │<──────────────────────────────────────────────────────┤
    │                             │                       │
    │ 6. Store token              │                       │
    │    in localStorage          │                       │
    │                             │                       │
    │ 7. GET /api/user-data       │                       │
    │    Authorization: Bearer    │                       │
    ├──────────────────────────────────────────────────────>│
    │                             │                       │
    │                             │  8. Verify token      │
    │                             │     Query database    │
    │                             │                       │
    │ 9. Return user data         │                       │
    │<──────────────────────────────────────────────────────┤
    │                             │                       │
    │ 10. Render dashboard        │                       │
    │                             │                       │
```

---

## 📦 Component Structure

```
COPCCA CRM
│
├── Frontend (React + TypeScript + Tailwind)
│   │
│   ├── /App.tsx (Main component)
│   │
│   ├── /components/
│   │   ├── LoginForm.tsx
│   │   ├── Dashboard.tsx
│   │   ├── AfterSalesModule.tsx
│   │   ├── KPIModule.tsx
│   │   ├── CompetitorModule.tsx
│   │   ├── StrategyModule.tsx
│   │   ├── DebtModule.tsx
│   │   ├── TaskModule.tsx
│   │   ├── AIAssistant.tsx
│   │   ├── Sidebar.tsx
│   │   └── SubscriptionGate.tsx
│   │
│   ├── /lib/
│   │   ├── supabase-client.ts
│   │   ├── auth-context.tsx
│   │   ├── api.ts
│   │   ├── use-data.ts
│   │   └── utils.ts
│   │
│   └── /utils/
│       └── supabase/
│           └── info.tsx (Supabase credentials)
│
└── Backend (Supabase Edge Functions)
    │
    ├── /supabase/functions/server/
        │
        ├── index.tsx (Main server)
        ├── kv_store.tsx (Database utilities)
        ├── whatsapp.tsx (WhatsApp integration)
        ├── tasks.tsx (Task endpoints)
        ├── reports.tsx (Reporting)
        ├── subscription.tsx (Billing)
        └── email-service.tsx (Email)
```

---

## 🔒 Security Architecture

```
┌────────────────────────────────────────────────────────���─────┐
│                    Security Layers                            │
└──────────────────────────────────────────────────────────────┘

Layer 1: Transport Security
  └─ HTTPS/TLS encryption for all connections
     - DigitalOcean provides SSL certificate
     - Supabase enforces HTTPS

Layer 2: Authentication
  └─ JWT-based authentication
     - Tokens expire after session
     - Refresh tokens for extended sessions
     - Password hashing (bcrypt)

Layer 3: Authorization
  └─ User-based access control
     - Each user can only see their own data
     - Admin role for special operations
     - API validates user ID on every request

Layer 4: API Security
  └─ CORS configuration
     - Only allowed origins
     - Specific headers and methods
     - Credentials included

Layer 5: Data Security
  └─ Environment variables encrypted
     - Service role key never exposed to frontend
     - Database credentials stored securely
     - Sensitive data encrypted at rest

Layer 6: Network Security
  └─ Edge network protection
     - DDoS protection
     - Rate limiting
     - Request validation
```

---

## 💾 Data Storage Strategy

```
┌───────────────────────────────────────────────────────────────┐
│                  Data Storage Locations                       │
└───────────────────────────────────────────────────────────────┘

User Sessions
  └─ Supabase Auth (automatic)
     - JWT tokens
     - Session management
     - Password reset tokens

Application Data
  └─ kv_store_a2294ced table (PostgreSQL)
     - User profiles
     - Follow-up records
     - KPI data
     - Competitor info
     - Strategies
     - Debt records
     - Tasks
     - Subscriptions

Files (if needed)
  └─ Supabase Storage
     - User uploads
     - Report PDFs
     - Export files

Frontend State
  └─ Browser localStorage
     - JWT access token
     - User preferences
     - Theme settings
     - Cached data
```

---

## 🔄 State Management

```
┌───────────────────────────────────────────────────────────────┐
│                    State Flow                                 │
└───────────────────────────────��───────────────────────────────┘

Global State (React Context)
  │
  ├─ AuthContext
  │  └─ Current user
  │     Access token
  │     User permissions
  │
  ├─ CurrencyContext
  │  └─ Selected currency
  │     Exchange rates
  │
  └─ LoadingContext
     └─ Loading states
        Error states

Component State (useState)
  │
  ├─ Form inputs
  ├─ Modal visibility
  ├─ Filter settings
  └─ Temporary UI state

Server State (Custom hooks)
  │
  ├─ useFollowUpData()
  ├─ useKPIData()
  ├─ useDebtData()
  ├─ useTaskData()
  └─ useSubscriptionData()
```

---

## 🎯 Performance Optimization

```
Frontend Optimizations
  │
  ├─ Code Splitting
  │  └─ Lazy loading modules
  │     Dynamic imports
  │
  ├─ Asset Optimization
  │  └─ Minified JS/CSS
  │     Compressed images
  │     Tree shaking
  │
  ├─ Caching
  │  └─ Service Worker (PWA)
  │     LocalStorage caching
  │     API response caching
  │
  └─ CDN Delivery
     └─ DigitalOcean CDN
        Global edge locations

Backend Optimizations
  │
  ├─ Database
  │  └─ Indexed queries
  │     Connection pooling
  │
  ├─ Edge Functions
  │  └─ Global deployment
  │     Cold start optimization
  │
  └─ Caching
     └─ In-memory cache
        Query result caching
```

---

## 📱 Progressive Web App (PWA)

```
PWA Features
  │
  ├─ Installable
  │  └─ Add to Home Screen
  │     Desktop shortcut
  │
  ├─ Offline Support
  │  └─ Service Worker
  │     Cache-first strategy
  │     Background sync
  │
  ├─ Push Notifications
  │  └─ Reminder notifications
  │     Task alerts
  │     Debt follow-up alerts
  │
  └─ Mobile-First Design
     └─ Responsive layouts
        Touch-friendly UI
        Mobile navigation
```

---

## 🔌 External Integrations

```
Third-Party Services
  │
  ├─ WhatsApp Business API
  │  └─ Send messages
  │     Customer communication
  │
  ├─ Email Service (Resend)
  │  └─ Password reset
  │     Notifications
  │     Reports
  │
  └─ Currency Exchange API
     └─ Real-time rates
        Multi-currency support
```

---

## 📊 Monitoring & Logging

```
Monitoring Setup
  │
  ├─ Frontend
  │  └─ Browser console
  │     Error boundaries
  │     Performance monitoring
  │
  ├─ Backend
  │  └─ Supabase logs
  │     Edge function logs
  │     Database query logs
  │
  └─ Deployment
     └─ DigitalOcean logs
        Build logs
        Deploy logs
```

---

## 🎯 Scalability

```
Current Capacity
  │
  ├─ DigitalOcean BASIC Plan ($5/month)
  │  └─ 200+ concurrent users
  │     Automatic scaling
  │     CDN caching
  │
  └─ Supabase Free Tier
     └─ 500MB database
        2GB bandwidth/month
        50,000 monthly active users

Future Scaling
  │
  ├─ DigitalOcean PRO Plan ($12/month)
  │  └─ 1000+ concurrent users
  │     More resources
  │
  └─ Supabase Pro Plan ($25/month)
     └─ 8GB database
        50GB bandwidth/month
        100,000 monthly active users
```

---

This architecture is designed for:
- ✅ **High availability** (99.9% uptime)
- ✅ **Scalability** (starts small, grows as needed)
- ✅ **Security** (multiple layers of protection)
- ✅ **Performance** (fast loading, responsive)
- ✅ **Cost-effective** (pay as you grow)

**Total Infrastructure Cost:** $5-10/month to start! 🚀
